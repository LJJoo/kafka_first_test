# Docker Compose 환경변수 예시
# 복사 후 .env.docker 로 저장하여 사용
# cp .env.docker.example .env.docker

# DB
DB_NAME=kafka_test
DB_USER=root
DB_PASS=your_password

# Kafka CLI 외부 접속 주소
# 로컬 테스트: localhost
# 서버 배포: 실제 서버 IP 또는 도메인
KAFKA_EXTERNAL_HOST=localhost
